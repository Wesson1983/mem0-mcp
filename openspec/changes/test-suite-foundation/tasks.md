## 1. Project scaffolding and configuration

- [x] 1.1 Add `pytest-asyncio>=0.23.0` to `[dependency-groups].dev` in `pyproject.toml` (alongside the existing `pytest>=8.3.4`, `ruff>=0.7.0`, `mypy>=1.18.2`). Pick a concrete version published at least 7 days ago. Verify `pip install -e ".[dev]"` succeeds with no resolver conflict against `pytest>=8.3.4`, `pytest --version` reports `pytest-asyncio` as a registered plugin, and `pip check` reports no broken requirements.
- [x] 1.2 Create the `tests/` package tree first (before pointing pytest at it): `tests/__init__.py`, `tests/unit/__init__.py`, `tests/integration/__init__.py`, `tests/e2e/__init__.py`, all empty. Verify the directories exist.
- [x] 1.3 Add `[tool.pytest.ini_options]` to `pyproject.toml` with `asyncio_mode = "auto"`, `testpaths = ["tests"]`, and `markers = ["e2e: requires MEM0_E2E=1 plus a running container, mem0 OSS, and LM Studio"]`. Ordered after 1.2 because `testpaths` pointing at a missing directory makes pytest exit with an error, not a clean empty collection. Verify `pytest --collect-only` exits 0 collecting zero tests, and `pytest --markers` lists the `e2e` marker.

## 2. Fake HTTP server fixture

- [x] 2.1 Create `tests/conftest.py` with a `fake_mem0_server` fixture: a Starlette app with routes matching the mem0 OSS REST surface (`POST /memories`, `POST /search`, `GET /memories`, `GET /memories/{id}`, `PUT /memories/{id}`, `DELETE /memories`, `DELETE /memories/{id}`, `GET /memories/{id}/history`, `GET /entities`, `DELETE /entities/{type}/{id}`). Each handler reads a mutable `responses` dict for status code + body, sleeps for the configured latency (`write_latency` for POST/PUT/PATCH/DELETE, `read_latency` for GET), and returns the canned response. Every handler MUST also record the received request (method, path, query params, JSON body, and the `X-API-Key` / `Content-Type` headers) into a `received` list on the config object, and echo those request details back inside the response body — tasks 7.5, 8.4, and 9.2 assert against this echo. Append to `received` under a `threading.Lock` since handlers run on uvicorn worker threads while tests read from the test thread. Verify the app module imports without error.
- [x] 2.2 Complete the `fake_mem0_server` fixture: start `uvicorn.Server` in a background daemon thread with `host="127.0.0.1"`, `port=0`, `log_level="warning"`. Poll `server.servers[0].sockets` until the socket is bound, bounded by a 5-second deadline — on timeout raise `RuntimeError("fake mem0 server failed to bind within 5s")` so a startup failure fails fast instead of hanging the suite. Then read the assigned port from the bound socket via `getsockname()[1]`. Yield `(base_url, config)` where `config` exposes the mutable `responses` dict and latency settings. In teardown, set `server.should_exit = True` and `thread.join(timeout=5)`; if the thread is still alive after the join, log a warning. Verify a test that issues `requests.get(f"{base_url}/entities")` receives the canned response, and verify the timeout path by asserting the fixture raises `RuntimeError` (not hangs) when given a deliberately unstartable app.

## 3. Unit tests — pure helpers

- [x] 3.1 Create `tests/unit/test_helpers.py` with tests for `_validate_base_url`: accepts `http://localhost:8888`, `https://api.example.com`, `http://127.0.0.1`, `http://host.docker.internal`; rejects missing scheme (`localhost:8888`), non-HTTP scheme (`ftp://...`), non-local host without HTTPS (`http://api.example.com`); strips trailing slash. Verify all assertions pass.
- [x] 3.2 Add tests for `_redact`: redacts `api_key` patterns, `token` patterns, `bearer` tokens, `authorization` headers; truncates to the limit; leaves non-sensitive text unchanged. Verify all assertions pass.
- [x] 3.3 Add tests for `_validate_memory_id`: accepts alphanumeric + `_` + `-`; rejects empty string, slashes, spaces, special characters. Verify all assertions pass.
- [x] 3.4 Add tests for `_error`: returns `{"error": code, "detail": detail}` without status; returns `{"error": code, "detail": detail, "status": status}` with status. Verify all assertions pass.
- [x] 3.5 Add tests for `_int_env`: returns the env value when set and valid; returns the default when unset; returns the default when set to a non-integer (and logs a warning). Use `monkeypatch.setenv`/`monkeypatch.delenv`. Verify all assertions pass.
- [x] 3.6 Add tests for `_with_default_filters`: injects `user_id` when absent; injects `agent_id` when absent and `default_agent` is set; preserves caller-supplied `user_id` and `agent_id`; handles `None` filters input. Verify all assertions pass.

## 4. Unit tests — `_resolve_settings`

- [x] 4.1 Create `tests/unit/test_resolve_settings.py` with tests for env-only resolution: `ENV_API_KEY` + `ENV_BASE_URL` + `ENV_DEFAULT_USER_ID` resolve correctly; `ENV_API_KEY = None` raises `RuntimeError`. **`_resolve_settings` reads module-level constants captured at import time, not `os.getenv`** — so use `monkeypatch.setattr(server, "ENV_API_KEY", ...)` etc., not `monkeypatch.setenv`. Pass a stub `Context` whose `session_config` is `None`. Also cover `ctx=None` (the tool default) since `getattr(None, "session_config", None)` must resolve to `None` without raising. Verify all assertions pass.
- [x] 4.2 Add tests for env-over-session-config precedence: when both env and session config are set for `api_key`, `base_url`, `default_user_id`, `default_agent_id`, the env value wins. Assert the resolved tuple holds env values, not session-config values. Log-output assertions are a documented non-goal — do not assert on the warning text, only on the resolved values.
- [x] 4.3 Add tests for session-config fallback: with `ENV_API_KEY`/`ENV_BASE_URL`/`ENV_DEFAULT_AGENT_ID` patched to `None`, session-config values are used. Cover both session-config shapes `_config_value` supports — a plain `dict` and an object with attributes — since the helper branches on `isinstance(source, dict)`. Note `ENV_DEFAULT_USER_ID` carries the built-in `"mem0-mcp"` default, so a session-config `default_user_id` is always overridden; assert that documented behavior rather than a fallback. Verify the returned tuple has session-config values for the other three fields.

## 5. Unit tests — client cache

- [x] 5.1 Create `tests/unit/test_client_cache.py` with tests for `_client`: same `(base_url, api_key)` returns the same `Mem0OSSClient` instance (identity check with `is`); different `api_key` returns a different instance; different `base_url` returns a different instance. For eviction, monkeypatch `server._CLIENT_CACHE_MAX` to a small value (e.g. 2) instead of creating 32 real clients, then assert the oldest key is dropped and `len(_CLIENT_CACHE)` never exceeds the max. Call `clear_client_cache()` in a fixture before and after each test so cache state does not leak between tests. Verify all assertions pass.
- [x] 5.2 Add tests for `clear_client_cache`: clears the cache so subsequent `_client` calls create new instances. Verify all assertions pass.

## 6. Unit tests — schemas

- [x] 6.1 Create `tests/unit/test_schemas.py` with tests for `AddMemoryArgs`: accepts `text` or `messages`; `model_dump(exclude_none=True)` omits unset optional fields; `ToolMessage` validates `role`/`content` structure. Verify all assertions pass.
- [x] 6.2 Add tests for `SearchMemoriesArgs`, `GetMemoriesArgs`, `DeleteAllArgs`, `DeleteEntitiesArgs`, `UpdateMemoryArgs`: each schema accepts valid inputs; `model_dump(exclude_none=True)` produces the expected payload shape (flat key-value, no nested wrappers for None fields). Verify all assertions pass.

## 7. Integration tests — `Mem0OSSClient`

- [x] 7.1 Create `tests/integration/test_mem0_oss_client.py` with a test for `Mem0OSSClient._call` happy path: `GET /entities` against the fake server returns the canned JSON. Verify the response matches the canned body.
- [x] 7.2 Add tests for HTTP error mapping: `GET` returning 404 → `_error("http_404", ...)`; `POST` returning 500 → `_error("http_500", ...)`; response includes `status` field. Override the fake server's `responses` dict per-test. Verify all assertions pass.
- [x] 7.3 Add a test for `requests.RequestException` handling: point a `Mem0OSSClient` at a closed port on `127.0.0.1` (bind a socket to get a free port, close it, use that port) so `requests` raises `ConnectionError`. Verify `_call` returns `_error("http_request_failed", ...)` with no `status` key, and verify `clear_client_cache` was invoked (populate `_CLIENT_CACHE` via `_client(...)` first, then assert it is empty after the failed call). Do not attempt to make the fake server drop connections mid-response — the closed-port approach is deterministic and needs no server support.
- [x] 7.4 Add a test for timeout selection: monkeypatch `server._WRITE_TIMEOUT` and `server._READ_TIMEOUT` to small distinct sentinel values, wrap `requests.Session.request` with a spy that records the `timeout` kwarg it received, and assert `POST`/`PUT` calls pass the write sentinel while `GET` calls pass the read sentinel. Note that `delete_all` passes `timeout=_WRITE_TIMEOUT` explicitly even though its method is `DELETE` — assert that too, since `DELETE` otherwise falls into the read branch (`_call` selects the write timeout only for `POST`/`PUT`/`PATCH`). Do not use wall-clock sleeps for this — the spy is deterministic and fast.
- [x] 7.5 Add a test for `functools.partial` kwarg survival (refactor-specific): assert that `method`, `url`, `params`, and `json` all reach the wire by reading the fake server's `received` echo (task 2.1) — verify the recorded method, path, query params, and JSON body match what `_call` was given. `timeout` is not observable on the wire, so it is covered separately by the spy in task 7.4. This test is green on sync code (direct call) and must remain green after the refactor (partial binding); it is the guard against a misbound `functools.partial` silently dropping an argument.
- [x] 7.6 Add happy-path tests for all 10 wrapper methods (`add`, `search`, `list_memories`, `get`, `update`, `delete`, `delete_all`, `history`, `list_entities`, `delete_entity`) against the fake server. Each test calls the wrapper and verifies the canned response is returned. Verify all 10 tests pass.

## 8. Integration tests — tool functions

- [x] 8.1 Create `tests/integration/test_tool_functions.py` with a happy-path test for `add_memory`: point the server at the fake server by patching the module constant (`monkeypatch.setattr(server, "ENV_BASE_URL", base_url)` and `ENV_API_KEY`), not `monkeypatch.setenv` — these are captured at import time. The 10 tool functions are closures defined inside `create_server()` and are not importable module attributes, so add a fixture that calls `create_server()` and extracts the underlying callables from the returned `FastMCP` instance's tool registry (via its tool manager), then invokes them directly with a stub `Context`. Clear `_CLIENT_CACHE` between tests so a stale cached client bound to an old port is never reused. Verify `add_memory(text="...")` returns the canned JSON.
- [x] 8.2 Add happy-path tests for the remaining 9 tool functions (`search_memories`, `get_memories`, `delete_all_memories`, `list_entities`, `get_memory`, `get_memory_history`, `update_memory`, `delete_memory`, `delete_entities`). Each test calls the tool with valid args and verifies the canned response. Verify all 9 tests pass.
- [x] 8.3 Add error-path tests for tool-level validation: `add_memory` with neither `text` nor `messages` → `_error("messages_missing", ...)`; `update_memory` with no fields → `_error("nothing_to_update", ...)`; `delete_entities` with no scope → `_error("scope_missing", ...)`; `get_memory`/`delete_memory`/`get_memory_history`/`update_memory` with invalid `memory_id` → `_error("invalid_memory_id", ...)`; `add_memory` with malformed `messages` → `_error("invalid_messages", ...)`. Verify all error-path tests pass.
- [x] 8.4 Add a test for default user/agent injection: with `MEM0_DEFAULT_USER_ID` and `MEM0_DEFAULT_AGENT_ID` set, `add_memory(text="...")` sends a payload containing both `user_id` and `agent_id`; `get_memories()` sends params containing both; `search_memories(query="...")` sends a body with `filters` containing both. Verify by reading the fake server's `received` echo (task 2.1). Note that `server.py` reads `MEM0_DEFAULT_USER_ID` / `MEM0_DEFAULT_AGENT_ID` into module-level constants (`ENV_DEFAULT_USER_ID`, `ENV_DEFAULT_AGENT_ID`) at import time, so `monkeypatch.setenv` alone has no effect — monkeypatch the module attributes directly (`monkeypatch.setattr(server, "ENV_DEFAULT_AGENT_ID", ...)`) or reload the module. Verify all assertions pass.

## 9. Integration tests — concurrency (the refactor's thesis)

- [x] 9.1 Create `tests/integration/test_concurrency.py` with `test_event_loop_yields_during_write_read_in_parallel`: configure the fake server with `write_latency=2.0`, `read_latency=0.05`. Create a `Mem0OSSClient` against the fake server. Start `client.add({...})` as a background task, `await asyncio.sleep(0.1)` to let the write enter its blocking I/O, then `await client.list_memories({"user_id": "u"})` and measure elapsed time with `time.monotonic()`. Assert `read_elapsed < 1.0` — comfortably below the 2.0s write latency yet loose enough to absorb CI scheduler jitter and thread-pool handoff (a 0.5s bound is tight enough to flake on a loaded runner). Await the write task and assert it returned the canned write response. **This test is RED on sync code** (the read blocks until the ~2.05s write completes) and **GREEN on async code**. Mark it `xfail(strict=True)` while the server is still sync so the suite stays green, with a comment stating the marker must be removed by `async-tool-execution`; `strict=True` means the marker itself fails once the refactor makes the test pass, forcing its removal. Verify the test fails (as xfail) against current sync code — this is the characterization proof that the refactor is needed.
- [x] 9.2 Add `test_concurrent_calls_same_cached_client`: make the fake server echo the received `user_id` query param back in its response body, then fire 10 concurrent `client.list_memories({"user_id": f"u{i}"})` calls through the same `Mem0OSSClient` instance (same `requests.Session`) using `asyncio.gather`. Assert all 10 complete without exceptions, and assert each response echoes back its own `user_id` (`results[i]` corresponds to `u{i}`) — this catches response cross-talk between concurrent calls sharing one session, which a "no exceptions" assertion alone would miss. Verify the test passes (concurrent reads on the same session are low-risk per the design).

## 10. E2e tests — MCP transport

- [x] 10.1 Create `tests/e2e/conftest.py` (named `conftest.py`, not `conftest_e2e.py` — pytest only auto-discovers fixtures from files named `conftest.py`) with an autouse `skip_if_no_e2e` fixture that calls `pytest.skip("requires MEM0_E2E=1 plus a running container, mem0 OSS, and LM Studio")` when `MEM0_E2E != "1"`, and applies the `e2e` marker to the whole package via `pytestmark = pytest.mark.e2e` so `-m "not e2e"` deselects it. Add an `mcp_session` fixture that performs the MCP handshake (POST `/mcp` `initialize` → capture the `mcp-session-id` response header → POST `notifications/initialized`) against `MCP_URL` (default `http://localhost:8765/mcp`) with `Accept: application/json, text/event-stream`, and yields a helper exposing `call(tool, arguments)`. Reuse the SSE-parsing logic already proven in `verify_mcp.py`'s `parse_sse` (extract the first JSON-RPC `result`/`error` from `data:` lines) — factor it into a shared helper rather than reimplementing it. Add a `test_scope` fixture generating `user_id=f"test_e2e_{uuid4().hex}"` and `agent_id=f"test_e2e_{uuid4().hex}"`, exposing a mutable dict so tests can hand memory IDs to later tests, and in `finally` calling `delete_entities` for the user scope, catching and logging any exception so cleanup failure never masks a test failure. Verify the fixtures import without error and that the suite is fully skipped when `MEM0_E2E` is unset.
- [x] 10.2 Create `tests/e2e/test_mcp_transport.py` with `test_initialize_and_tools_list`: perform the MCP handshake, call `tools/list`, assert 10 tools are returned. Verify the test passes when `MEM0_E2E=1` and the container is running.
- [x] 10.3 Add `test_auth_failure`: call `tools/call list_entities` with a bad API key (override `MEM0_API_KEY` in the session config or env), assert the response payload contains `http_401`. Verify the test passes.

## 11. E2e tests — tool round-trips

- [x] 11.1 Create `tests/e2e/test_tools_call_round_trip.py` with `test_add_memory`: call `tools/call add_memory` with `text="test e2e memory"` under the test scope, assert the response is not an error and contains a memory ID. Verify the test passes.
- [x] 11.2 Add `test_search_memories`: call `tools/call search_memories` with `query="test"` and the test scope, assert non-error response with results. Verify the test passes.
- [x] 11.3 Add `test_get_memories`: call `tools/call get_memories` with the test scope, assert non-error response. Verify the test passes.
- [x] 11.4 Add `test_get_memory`: read the memory ID that `test_add_memory` stored in the `test_scope` fixture's mutable dict, call `tools/call get_memory` with it, assert non-error response. Do not rely on file-order test execution — if the ID is absent (because `test_add_memory` failed or was deselected), `pytest.skip` with a clear reason rather than failing on a `KeyError`. Verify the test passes.
- [x] 11.5 Add `test_get_memory_history`: call `tools/call get_memory_history` with the memory ID, assert non-error response. Verify the test passes.
- [x] 11.6 Add `test_update_memory`: call `tools/call update_memory` with the memory ID and new text, assert non-error response. Verify the test passes.
- [x] 11.7 Add `test_delete_memory`: call `tools/call delete_memory` with the memory ID, assert non-error response. Order this after `test_get_memory_history` and `test_update_memory` (which need the memory to still exist) — express the dependency through the shared `test_scope` dict plus skip-if-absent, as in task 11.4, not through implicit file order. Verify the test passes.
- [x] 11.8 Add `test_delete_all_memories`: call `tools/call delete_all_memories` with the test scope, assert non-error response. Verify the test passes.
- [x] 11.9 Add `test_list_entities`: call `tools/call list_entities`, assert non-error response containing the test scope's user/agent. Verify the test passes.
- [x] 11.10 Add `test_delete_entities`: call `tools/call delete_entities` with the test scope's `user_id`, assert non-error response. Verify the test passes.

## 12. E2e tests — concurrency (real-latency proof)

- [x] 12.1 Create `tests/e2e/test_concurrent_read_during_write.py` with `test_read_completes_during_write`: open two MCP sessions. From session A, start `tools/call add_memory` (20-40s real write). `await asyncio.sleep(1)` to let the write enter its blocking I/O. From session B, call `tools/call get_memories` and measure elapsed time. Assert `read_elapsed < 5.0` (read should be seconds, not tens of seconds), `write_elapsed > 10.0` (prove the write was actually slow), and `read_elapsed < write_elapsed / 2` (read didn't wait for write). Verify the test passes when `MEM0_E2E=1` and the full stack is running.

## 13. CI workflow

- [x] 13.1 Create `.github/workflows/ci.yml` with a `lint-and-test` job that runs on `push` and `pull_request`. Steps: checkout, setup Python 3.12, install dev dependencies (`pip install -e ".[dev]"`), `ruff check`, `mypy src/`, `pytest -m "not e2e"`. Verify the workflow file is valid YAML and the job steps are syntactically correct.
- [x] 13.2 Verify the full local run: `ruff check && mypy src/ && pytest -m "not e2e"` passes with all unit and integration tests green against the current sync code. This is the baseline that `async-tool-execution` must not break.

## 14. Final verification

- [ ] 14.1 Run `pytest -m "not e2e" --tb=short -q` and verify the run is green with exactly one `xfailed` test: `test_event_loop_yields_during_write_read_in_parallel` (task 9.1). Then run `pytest tests/integration/test_concurrency.py::test_event_loop_yields_during_write_read_in_parallel -rx` and verify the xfail reason shows the timing assertion (`read_elapsed < 1.0`) failing — proving the read blocked behind the write on sync code, not that the test errored for an unrelated reason. Zero `failed`, zero `error`, zero `xpassed`.
- [ ] 14.2 Run `openspec validate --changes "test-suite-foundation"` (the flag is `--changes`, not `--change`) and verify it reports a pass. Equivalent form: `openspec validate test-suite-foundation --type change`.
- [ ] 14.3 Add a "Prerequisite" note to `openspec/changes/async-tool-execution/proposal.md` and `openspec/changes/batch-write-guardrails/proposal.md` recording that `test-suite-foundation` must be applied and green first. OpenSpec has no mechanical cross-change prerequisite field, so this is documentation only — verify by reading back that both proposals name `test-suite-foundation`. Also record in `async-tool-execution/tasks.md` that removing the `xfail(strict=True)` marker from task 9.1's test is part of that change's work.
